
const loginReducer = (state, action) => {
    switch (action.type) {
        case "LOGIN": {
            break;
        }

        case "LOGIN_SUCCESS": {
            break;
        }

        default:
            return state;
    }
}

export default loginReducer;