
const userReducer = (state, action) => {
    switch (action.type) {
        case "FETCH_USER": {
            break;
        }

        case "FETCH_USER_SUCCESS": {
            break;
        }

        default:
            return state;
    }
}

export default userReducer;